node_modules
/dist
# Keep environment variables out of version control
.env

/generated/prisma

# Uploaded dish photos (see MenuService.setDishPhoto) - served locally via
# app.useStaticAssets in main.ts, not part of the repo.
/uploads
